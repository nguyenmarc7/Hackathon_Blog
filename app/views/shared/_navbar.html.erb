	  <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-normal">Blog Hub</h5>
  <nav class="my-2 my-md-0 mr-md-3">
  <% if(!user_signed_in?) %>
    <a class="p-2 text-dark" href="#"><%= link_to "Log In", new_user_session_path %></a>
    <% end %>
   <% if(user_signed_in?) %>
    <a class="p-2 text-dark" href="#"><%= link_to "Create New Post", new_post_path, class:"navbar-item" %></a>
      <a class="" href="#"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete%></a>
    
        <% end %>
    <a class="p-2 text-dark" href="#"><%= link_to 'Home', root_path, class: "navbar-item" %></a>
  
  </nav>
    <% if(!user_signed_in?) %>
  <a class="p-2 text-dark" href="#"><%= link_to 'Sign Up', new_user_registration_path%></a>
      <% end %>

</div>